description: Add enum value to new schema without default
check:
  compatibility: "BACKWARD"
  compatible: true
schema:
  type: "AVRO"
  oldschema: |
    { "namespace": "example.avro",
      "type": "record",
      "name": "User",
      "fields": [
        { "name": "name",
          "type": "string" },
        { "name": "favorite_color", 
          "type": {
            "type": "enum",
            "name": "ColorEnum",
            "symbols": ["unknown", "red", "blue", "green"]
          }
        }
      ]
    }
  newschema: |
    { "namespace": "example.avro",
      "type": "record",
      "name": "User",
      "fields": [
        { "name": "name",
          "type": "string" },
        { "name": "favorite_color", 
          "type": {
            "type": "enum",
            "name": "ColorEnum",
            "symbols": ["unknown", "red", "blue", "green", "yellow"]
          }
        }
      ]
    }
