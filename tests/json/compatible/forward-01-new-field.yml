description: New field (additionalProperties=true)
check:
  compatibility: "FORWARD"
  compatible: true
schema:
  type: "JSON"
  oldschema: |
    {"type": "object",
      "properties": {
        "name": {"type": "string"}
      },
      "required": ["name"]
    }
  newschema: |
    {"type": "object",
      "properties": {
        "name": {"type": "string"},
        "vorname": {"type": "string"}
      },
      "required": ["name"]
    }
